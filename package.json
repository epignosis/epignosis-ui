{
  "name": "epignosis-ui-workspace",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Epignosis shared UI workspace — tokens, React components, Vue components.",
  "engines": {
    "node": ">=20",
    "pnpm": ">=9"
  },
  "scripts": {
    "build": "pnpm -r --filter @epignosis-ui/react --filter @epignosis-ui/vue run build",
    "storybook:react": "pnpm --filter @epignosis-ui/react storybook",
    "storybook:vue": "pnpm --filter @epignosis-ui/vue storybook",
    "storybook:tokens": "pnpm --filter @epignosis-ui/tokens storybook",
    "storybook:all": "pnpm -r --parallel --filter @epignosis-ui/react --filter @epignosis-ui/vue --filter @epignosis-ui/tokens run storybook",
    "build-storybook": "pnpm -r --filter @epignosis-ui/react --filter @epignosis-ui/vue --filter @epignosis-ui/tokens run build-storybook"
  },
  "devDependencies": {
    "typescript": "^6.0.0"
  }
}
